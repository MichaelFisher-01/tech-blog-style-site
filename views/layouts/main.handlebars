<html lang='en'>
	<head>
		<meta charset='UTF-8' />
		<meta http-equiv='X-UA-Compatible' content='IE=edge' />
		<meta name='viewport' content='width=device-width, initial-scale=1.0' />
		<title>Document</title>
	</head>
	<nav>
		<a href='/'>Home</a>
		<a href='/dashboard'>Dashboard</a>
		{{#if loginStatus}}
			<button id='logout'>Logout</button>
		{{else}}
			<a href='/login'>Login</a>
		{{/if}}
	</nav>
	<div class='logo'>TechBlog
		<br />
		Currently Logged in as:
		{{currentUser}}</div>
	<body>

		{{{body}}}
		<!-- Home Screen errors out if you load this script without a logout button -->
		{{#if loginStatus}}
			<script src='/js/accounts/logout.js'></script>
		{{/if}}
	</body>

</html>