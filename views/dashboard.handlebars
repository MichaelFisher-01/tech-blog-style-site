<h2> Your Created Posts: </h2>
{{#if posts.length}}
	{{#each posts as |post|}}
		<div class='dashboardPosts'>
			<a href='/editPost/{{post.id}}'>{{post.title}}</a>
			<p>{{noTime post.updatedAt}}</p>
		</div>
	{{/each}}
{{/if}}
<div class='newPost'><a href='/createPost'>+ NEW POST</a></div>